---
title: "Get QR"
api: "GET https://api.softtopos.be/api/v1/website/qr"
description: "The Get QR method returns the QR code as a base64 encoded image, which can be directly added to your HTML img tag's src attribute."
---

### Response

<ResponseField name="qr" type="string">
  This is returning QR code as base64 encoded image
</ResponseField>

<ResponseField name="status" type="number">
  This indicates whether the call was successful or not. A status code of 200 is returned if the call was successful and a status code of 400 is returned if the call was not successful.
</ResponseField>

<RequestExample>

```bash Example Request
curl --location --request POST 'https://api.softtopos.be/api/v1/website/qr' \
--header 'Accept: application/json'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "qr": "data:image/svg+xml;base64,",
  "statusCode": 200
}
```

</ResponseExample>